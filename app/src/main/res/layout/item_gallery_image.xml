<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="data"
            type="com.easyhz.picly.domain.model.album.upload.GalleryImageItem" />
    </data>
    <FrameLayout
        android:id="@+id/frameLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/collectionViewCellBackground"
        android:layout_gravity="center_horizontal"
        android:layout_margin="0.5dp"
    >
        <ImageView
            android:id="@+id/galleryImageView"
            android:src="@drawable/default_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@{data.name}"
            app:imageUrl="@{data.uri}"
        />
        <View
            android:id="@+id/selectedView"
            android:visibility="gone"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/textFieldBackground40"
        />
        <TextView
            android:id="@+id/selectedTextView"
            android:visibility="gone"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_margin="4dp"
            android:gravity="center"
            android:background="@drawable/gallery_image_selected_button_shape"
            android:textColor="@color/mainText"
            android:fontFamily="@font/sf_pro_bold"
            android:text="n"
            android:textAlignment="center"
            android:layout_gravity="bottom|end"
        />
    </FrameLayout>
</layout>
